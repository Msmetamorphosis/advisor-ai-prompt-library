[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    metadata: validation of prompt.yaml metadata fields
    schema: validation of JSON schema files
    compliance: enforcement of compliance boundaries
    structure: enforcement of required-file structure
